# 캡스톤 디자인 실행 계획서

모바일 센서 기반 다중중재 치매 예방 및 보행 디지털 바이오마커 분석 플랫폼

> **문서 기준일: 2026-06-06**
> 본 결과물은 의료기기나 진단 서비스를 표방하지 않는 **연구·캡스톤 프로토타입**이다. 앱의 점수와 분석 결과는 건강 관리 및 연구용 참고 정보이며, 임상적 진단 정확도와 치료 효과는 별도의 데이터 수집과 검증 전에는 주장하지 않는다.

## 1. 프로젝트 개요

본 프로젝트는 초기 치매 및 경도인지장애(MCI)에 관심이 있는 사용자를 대상으로 인지 훈련, 운동, 감정 기록과 보호자 공유를 하나의 모바일 앱에서 제공하는 것을 목표로 한다. 기존 웹 중심 계획에서 Flutter 모바일 앱으로 전환하여 스마트폰 센서와 로컬 저장소를 활용하고, 필요한 기능에 한해 클라우드 및 생성형 AI를 연결한다.

### 현재 구현

- Flutter 기반 모바일 앱과 로컬 SQLite 기반 인증 및 사용자 데이터 저장
- 7종 인지 훈련, 규칙 기반 적응형 난이도, 점수 기록
- 기기 센서 기반 걸음 수 및 보행 지표 화면
- STT 결과에 대한 규칙 기반 음성 지표(TTR/WPM) 구현
- 감정 일기와 Google Gemini REST API 기반 AI 대화
- 기능별 Firebase Firestore 동기화: 보호자 공유, 이상 상태, 훈련 난이도 등
- 보호자 웹 대시보드, 관리자 화면, CS 기능
- 4페이지 임상 참고용 PDF 생성 및 공유

### 목표 아키텍처

현재 앱 중심 구조를 유지하면서 연구 검증이 필요한 분석 기능을 독립적으로 확장한다. FastAPI 기반 분석 서비스와 Node.js 기반 비즈니스 API의 MSA는 현재 채택된 백엔드가 아니라 데이터 규모, 보안 요구, 모델 배포 필요성이 확인된 뒤 검토할 **향후 선택지**다.

## 2. 프로젝트 목표

### 위험 신호 참고 정보

현재 구현된 설문, 훈련 기록, 보행 지표와 규칙 기반 TTR/WPM 결과를 사용자가 확인할 수 있게 한다. 음성·보행 데이터로 인지 위험을 예측하는 ML/LLM 모델은 향후 연구 항목이며, 실제 대상자 데이터셋과 임상 기준을 이용한 민감도·특이도·재현성 검증 전에는 선별 정확도를 제시하지 않는다.

### 일상 루틴

짧은 인지 훈련, 걸음 수 확인, 감정 일기를 반복 가능한 일상 루틴으로 제공한다. FINGER 기반 수면·혈압·혈당·식단 입력과 Health Connect/HealthKit 실데이터 연동은 향후 범위다.

### 결과 요약 및 공유

사용자 기록을 차트와 요약 문장으로 보여주고 보호자와 공유한다. 현재 PDF 생성은 구현되어 있으며, CSV 내보내기와 병원 EMR 직접 연동은 구현되지 않았다.

### 검증 목표

- 규칙 기반 음성 점수와 향후 모델 결과를 분리하여 비교 가능한 평가 데이터셋 구축
- 주요 사용자 흐름의 성공률, 오류율, 완료 시간 측정
- 접근성 점검표와 실제 기기 테스트를 통한 글자 크기, 대비, 터치 영역 검증
- 대표 Android 기기에서 초기 화면 표시 시간과 훈련 화면 프레임 성능 측정

위 항목은 달성 사실이 아니라 캡스톤 평가 과정에서 측정할 목표다.

## 3. 핵심 기능 및 상세 내용

### 3.1 온보딩 및 인증

#### 현재 구현

회원가입과 로그인은 로컬 SQLite를 사용한다. 주민등록번호와 같은 불필요한 민감 식별 정보는 수집 대상에서 제외한다.

#### 향후 검증

건강 데이터 수집 및 보호자 공유 동의 문구를 기능별로 구분하고, 계정 복구와 서버 기반 인증이 필요해지는 시점에 별도 인증 백엔드를 검토한다.

### 3.2 음성 평가 및 AI 대화

#### 현재 구현

음성 평가 화면은 STT로 변환된 텍스트에서 어휘 다양성(TTR)과 분당 단어 수(WPM) 등 규칙 기반 지표를 계산한다. 이 값은 임상 진단이나 검증된 ML 예측값이 아니다. AI 대화 기능은 별도 기능으로 Google Gemini REST API를 사용하며, 음성 평가 점수를 Gemini가 판정하는 구조가 아니다.

#### 향후 연구

- 대명사 비율, 문장 완결성, 음향 특성 등 추가 특징 추출
- 수집 동의와 비식별화 절차를 포함한 평가 데이터셋 구축
- ML/LLM 모델 연결 및 규칙 기반 결과와의 비교
- 독립 검증 데이터에 대한 민감도, 특이도, 신뢰구간 보고

### 3.3 인지 훈련과 적응형 난이도

#### 현재 구현

난이도는 게임 카테고리별 1~10단계로 관리한다. 최근 3회 연속 정답이면 한 단계 올리되, 반응 시간이 제공된 게임은 최근 평균이 해당 단계의 목표 시간 이하인지도 확인한다. 최근 2회 연속 오답이면 한 단계 내린다. 변경된 난이도는 사용자명이 정상 바인딩된 경우 기능별 Firestore 문서에 동기화한다. 이는 규칙 기반 로직이며 AI가 사용자의 능력보다 일정 비율 높은 문제를 계산하는 방식이 아니다.

#### 향후 검증

규칙별 이탈률, 정답률, 완료 시간과 사용자 만족도를 측정하여 단계 조정 임계값을 보정한다.

### 3.4 생활 습관 및 보행 추적

#### 현재 구현

스마트폰 센서와 앱 내부 기록을 이용해 걸음 수와 보행 관련 지표를 표시한다. 현재 데이터만으로 임상적 디지털 바이오마커의 유효성을 주장하지 않는다.

#### 향후 연구

- Android Health Connect 및 iOS HealthKit 실제 권한·데이터 연동
- 걷는 동안 인지 과제를 수행하는 이중 과제 보행과 속도 저하율 분석
- FINGER 기반 수면, 혈압·혈당, 식단 입력 화면
- 보행 지표의 반복 측정 신뢰도와 외부 기준 대비 모델 검증

Health Connect, FINGER 생활 습관 입력, 이중 과제 보행, 보행·인지 예측 모델 검증은 모두 현재 완료 기능이 아니라 향후 연구 범위다.

### 3.5 주간 리포트 및 정서 케어

#### 현재 구현

훈련 및 활동 기록을 차트로 제공하고, 감정 일기를 달력 형태로 조회할 수 있다. AI 챗봇은 날짜·시간·날씨 문맥을 포함한 회상 중심 대화를 지원한다.

#### 향후 검증

AI 요약과 권고 문장의 사실성, 안전성, 반복성을 평가하고 의료 조언으로 오인될 수 있는 표현을 제한한다.

### 3.6 보호자 연계와 알림

#### 현재 구현

보호자 공유 데이터와 활동량 이상 상태를 Firestore에 기록하여 보호자 웹 대시보드에서 확인할 수 있다. 이상 감지 시 사용자 기기에는 로컬 알림을 표시하고, 비상 연락처가 있으면 SMS 앱으로 이동할 수 있는 동작을 제공한다.

#### 미구현

보호자 기기로 직접 전달되는 FCM/APNs 원격 푸시 알림은 구현되지 않았다. 따라서 현재 기능을 보호자 원격 푸시 전송으로 표현하지 않는다.

### 3.7 기관 연계와 내보내기

#### 현재 구현

앱에서 수집한 요약 정보를 4페이지 PDF로 생성하고 기기 공유 기능을 호출할 수 있다. 이 PDF는 진료 시 참고 자료이며 표준 EMR 문서나 의료기관 공식 기록을 의미하지 않는다.

#### 미구현

- CSV 내보내기
- 병원 EMR 직접 연동
- 임상 척도와 앱 지표 사이의 검증된 자동 매핑

## 4. 기술 구현 및 아키텍처

### 현재 구현

| 영역 | 구현 방식 |
|---|---|
| 클라이언트 | Flutter |
| 인증 | 로컬 SQLite 기반 회원가입·로그인 |
| 로컬 데이터 | SQLite |
| 클라우드 데이터 | 보호자 공유, 이상 상태, 난이도 등 기능별 Firestore 사용 |
| AI 대화 | 앱에서 Google Gemini REST API 호출 |
| 음성 평가 | STT 텍스트의 규칙 기반 TTR/WPM 계산 |
| 리포트 | 앱 내부 4페이지 PDF 생성 및 공유 |
| 알림 | 사용자 기기 로컬 알림, Firestore 대시보드 상태 공유 |

현재 구조는 모든 기능을 단일 서버 백엔드에 위임하지 않는다. 로컬 우선 기능과 기능별 Firestore/Gemini REST 연결이 공존한다.

### 목표 아키텍처

연구 데이터와 모델 배포 요구가 확정되면 다음 구조를 단계적으로 검토한다.

1. FastAPI 분석 서비스: 음성·보행 특징 추출 및 검증된 모델 추론
2. Node.js API: 서버 인증, 권한, 감사 로그, 기관 연계 등 일반 비즈니스 기능
3. 비식별 연구 저장소와 운영 데이터 저장소 분리
4. 원격 푸시 서비스와 보호자 수신 동의 관리
5. CSV 및 표준 기반 의료기관 내보내기

FastAPI/Node.js MSA는 목표 아키텍처의 후보이며 현재 운영 구조로 기술하지 않는다.

## 5. 성능·품질·연구 평가 계획

다음 수치는 보장값이 아니라 측정 가능한 **검증 목표**다.

| 항목 | 검증 목표 | 확인 방법 |
|---|---|---|
| 초기 화면 성능 | 대표 시험 기기에서 초기 화면 표시 1.5초 이내 | 릴리스 빌드 반복 측정 및 중앙값 기록 |
| 훈련 화면 성능 | 대표 시험 기기에서 목표 60fps, 저하 구간 별도 기록 | Flutter 성능 프로파일링 |
| 접근성 | WCAG 2.1 AA 관련 색 대비와 주요 모바일 접근성 항목 충족 목표 | 자동 검사와 수동 스크린리더 점검 |
| 기능 안정성 | 핵심 사용자 흐름의 자동 테스트와 실제 기기 회귀 테스트 | Flutter 테스트, Maestro, 수동 QA |
| 모델 성능 | 사전 정의된 데이터 분할과 지표로 성능 보고 | 독립 검증 세트, 민감도·특이도·신뢰구간 |

모델 성능 목표치는 데이터셋과 기준 라벨이 확정된 뒤 연구계획서에 별도로 정의한다. 검증 전에는 특정 정확도, 의료 효과, 성능 보장을 사용하지 않는다.

## 6. 기대 효과와 한계

### 기대 효과

- 사용자가 인지 훈련, 활동, 감정 기록을 한 앱에서 지속적으로 관리
- 보호자가 공유된 기록과 이상 상태를 웹 대시보드에서 확인
- PDF 요약 자료를 상담 또는 진료 시 참고 자료로 활용
- 향후 음성·보행 연구를 위한 프로토타입 데이터 흐름 검증

### 한계

- 의료기기 인허가나 임상적 진단 성능 검증이 완료되지 않음
- Health Connect/HealthKit, FINGER 입력, 이중 과제 보행은 향후 구현 항목
- 원격 보호자 푸시, CSV, EMR 연동은 미구현
- 음성 및 보행 ML/LLM 모델의 임상적 유효성은 검증되지 않음
